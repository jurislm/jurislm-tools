---
name: codebase-sync
description: 探索 codebase 現況，更新 README.md 與 CLAUDE.md，移除過時內容。
argument-hint: ""
---

Apply the `codebase-sync` skill to explore the current codebase and update documentation.

> 此 skill 操作當前目錄，無需傳入參數。

## Delegation

Follow the `codebase-sync` skill workflow: explore codebase → identify stale content → update README.md → update CLAUDE.md → validate.
