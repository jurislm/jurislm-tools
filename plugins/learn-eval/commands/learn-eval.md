---
name: learn-eval
description: Extract reusable patterns from the session, self-evaluate quality, and determine the right save location (Global vs Project).
argument-hint: "(no arguments — analyzes current session)"
---

Apply the `learn-eval` skill: review the session for extractable patterns → draft a skill file → run the quality gate checklist → determine save location (Global vs Project) → confirm with user → write the file.
